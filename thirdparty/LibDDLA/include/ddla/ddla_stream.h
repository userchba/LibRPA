#ifndef DDLA_STREAM_H
#define DDLA_STREAM_H

// ---------------------------------------------------------------------------
// Public header: forward-declares the opaque stream/handle type only.
// The concrete DdlaStream class lives in src/ddla_stream_impl.h (private).
// ---------------------------------------------------------------------------

namespace ddla {

class DdlaStream;

} // namespace ddla

#endif // DDLA_STREAM_H
